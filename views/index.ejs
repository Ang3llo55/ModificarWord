<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Generador de Documentos por Cédula</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .error-message {
            color: red;
            margin-top: 10px;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <h1>Generar Documento desde Plantilla</h1>
    <p>Introduce la cédula para buscar los datos y generar el documento usando la plantilla: <strong><%= defaultTemplate %></strong></p>

    <!-- Mensaje de error (si existe) -->
    <% if (error) { %>
        <p class="error-message"><%= error %></p>
    <% } %>

    <form action="/generate" method="POST">
        <label for="cedula">Cédula:</label><br>
        <input type="text" id="cedula" name="cedula" required><br><br>

        <button type="submit">Buscar Datos y Generar Documento</button>
    </form>

</body>
</html>